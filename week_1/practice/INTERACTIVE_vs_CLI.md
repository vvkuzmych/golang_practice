# Інтерактивний ввід vs Командний рядок (CLI)

## 🔀 Два підходи до вводу даних

### 📋 os.Args - Командний рядок (CLI)

**Використання:**
```bash
go run calculator.go 10 + 5
```

**Код:**
```go
if len(os.Args) < 4 {
    fmt.Println("Використання: calculator <число1> <операція> <число2>")
    return
}

num1 := os.Args[1]
operation := os.Args[2]
num2 := os.Args[3]
```

**Приклади програм з CLI:**
- `cli_basic/` - Базовий CLI
- `cli_calculator/` - Калькулятор CLI
- `solutions/solution_1.go` - Привітання CLI
- `solutions/solution_3.go` - TODO CLI

---

### 💬 Інтерактивний ввід - Діалог

**Використання:**
```bash
go run calculator.go
# Програма запитує:
Введіть перше число: 10
Введіть операцію: +
Введіть друге число: 5
# Результат: 15
```

**Код:**
```go
reader := bufio.NewReader(os.Stdin)

fmt.Print("Введіть перше число: ")
num1Str, _ := reader.ReadString('\n')
num1 := strings.TrimSpace(num1Str)
```

**Приклади програм з інтерактивним вводом:**
- `interactive_input/` - Базовий ввід
- `interactive_calculator/` - Калькулятор з меню
- `interactive_todo/` - TODO з меню
- `solutions/solution_1_interactive.go` - Привітання інтерактивне

---

## 📊 Порівняльна таблиця

| Критерій | CLI (os.Args) | Інтерактивний |
|----------|---------------|---------------|
| **Швидкість виконання** | ⚡⚡⚡ Дуже швидко | ⚡ Повільніше |
| **Зручність для початківців** | ⭐ Складно | ⭐⭐⭐⭐⭐ Легко |
| **Автоматизація** | ✅ Легко скриптувати | ❌ Важко |
| **Помилки** | ❌ Треба перезапускати | ✅ Можна виправити |
| **Валідація** | ⚠️ Важко реалізувати | ✅ Легко |
| **Довідка** | ⚠️ Треба пам'ятати | ✅ Вбудована |
| **Для досвідчених** | ✅ Зручно | ❌ Повільно |
| **Для новачків** | ❌ Незрозуміло | ✅ Інтуїтивно |

---

## 🎯 Коли використовувати що?

### ✅ Використовуйте CLI (os.Args) коли:

1. **Пишете утиліту для розробників**
   ```bash
   git commit -m "message"
   docker run -p 8080:80 nginx
   ```

2. **Потрібна автоматизація**
   ```bash
   for file in *.txt; do
       myprogram process $file
   done
   ```

3. **Швидкість важлива**
   ```bash
   # Одна команда замість діалогу
   calc 10 + 5
   ```

4. **Користувачі технічно підковані**

---

### ✅ Використовуйте інтерактивний ввід коли:

1. **Програма для звичайних користувачів**
   - Анкети
   - Опитування
   - Реєстрація

2. **Багато параметрів**
   - Форми з 10+ полями
   - Складні налаштування

3. **Потрібна валідація**
   ```
   Введіть вік (0-120): 150
   ❌ Помилка! Спробуйте ще раз
   Введіть вік (0-120): 25
   ✅ Прийнято
   ```

4. **Меню або вибір**
   ```
   1 - Додати
   2 - Видалити
   3 - Показати
   Ваш вибір:
   ```

---

## 💡 Комбінований підхід (Найкраще!)

Підтримуйте обидва методи:

```go
package main

import (
    "bufio"
    "fmt"
    "os"
    "strconv"
    "strings"
)

func main() {
    var num1, num2 float64
    var operation string
    
    // Якщо є аргументи - CLI режим
    if len(os.Args) == 4 {
        num1, _ = strconv.ParseFloat(os.Args[1], 64)
        operation = os.Args[2]
        num2, _ = strconv.ParseFloat(os.Args[3], 64)
        
        fmt.Println("📝 CLI режим")
    } else {
        // Інакше - інтерактивний режим
        reader := bufio.NewReader(os.Stdin)
        
        fmt.Println("📝 Інтерактивний режим")
        
        fmt.Print("Перше число: ")
        num1Str, _ := reader.ReadString('\n')
        num1, _ = strconv.ParseFloat(strings.TrimSpace(num1Str), 64)
        
        fmt.Print("Операція (+, -, *, /): ")
        operation, _ = reader.ReadString('\n')
        operation = strings.TrimSpace(operation)
        
        fmt.Print("Друге число: ")
        num2Str, _ := reader.ReadString('\n')
        num2, _ = strconv.ParseFloat(strings.TrimSpace(num2Str), 64)
    }
    
    // Обчислення (спільне для обох режимів)
    var result float64
    switch operation {
    case "+":
        result = num1 + num2
    case "-":
        result = num1 - num2
    case "*":
        result = num1 * num2
    case "/":
        if num2 != 0 {
            result = num1 / num2
        }
    }
    
    fmt.Printf("\nРезультат: %.2f %s %.2f = %.2f\n", num1, operation, num2, result)
}
```

**Використання:**
```bash
# CLI режим
$ go run main.go 10 + 5
📝 CLI режим
Результат: 10.00 + 5.00 = 15.00

# Інтерактивний режим
$ go run main.go
📝 Інтерактивний режим
Перше число: 10
Операція (+, -, *, /): +
Друге число: 5
Результат: 10.00 + 5.00 = 15.00
```

---

## 📁 Структура проекту

```
week_1/practice/
├── CLI режим:
│   ├── cli_basic/              os.Args приклад
│   ├── cli_calculator/         Калькулятор CLI
│   └── formatting/             fmt.Printf практика
│
└── Інтерактивний режим:
    ├── interactive_input/      Базовий ввід
    ├── interactive_calculator/ Калькулятор з меню
    └── interactive_todo/       TODO з меню
```

---

## 🧪 Тестування

### CLI програм:
```bash
cd practice/cli_calculator
go run main.go 10 + 5
go run main.go 20 mul 3
go run main.go 100 / 4
```

### Інтерактивних програм:
```bash
cd practice/interactive_calculator
go run main.go
# Слідуйте інструкціям на екрані
```

---

## 💻 Приклади з реального світу

### CLI інструменти:
```bash
# Git
git commit -m "message"

# Docker
docker run -d -p 8080:80 nginx

# Grep
grep "pattern" file.txt

# Go
go build -o myapp main.go
```

### Інтерактивні програми:
```bash
# NPM ініціалізація
npm init
# Запитує: name? version? description?

# Python pip
pip install package
# Може запитати підтвердження

# Установщики
./install.sh
# Показують меню і опції
```

---

## 📚 Додаткові матеріали

### Детальна теорія:
- `theory/06_user_input_methods.md` - Повний гайд по вводу

### CLI програми:
- `practice/cli_basic/` - Базові приклади
- `practice/cli_calculator/` - Калькулятор
- `solutions/solution_1.go` - Привітання
- `solutions/solution_3.go` - TODO менеджер

### Інтерактивні програми:
- `practice/interactive_input/` - Приклад вводу
- `practice/interactive_calculator/` - Калькулятор
- `practice/interactive_todo/` - TODO менеджер
- `solutions/solution_1_interactive.go` - Привітання

---

## ✅ Рекомендації

1. **Для навчання**: Почніть з інтерактивного вводу
2. **Для власних проєктів**: Використовуйте комбінований підхід
3. **Для CLI утиліт**: os.Args
4. **Для програм з UI**: Інтерактивний ввід

---

## 🎓 Завдання

1. Перепишіть будь-яку CLI програму в інтерактивну
2. Створіть програму з комбінованим підходом
3. Додайте валідацію в інтерактивні програми
4. Створіть меню для вашого TODO менеджера

---

**Успіхів у навчанні! 🚀**

