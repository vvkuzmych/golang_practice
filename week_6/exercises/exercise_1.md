# Exercise 1: ООП і Патерни Проєктування

## 🎯 Мета

Застосувати принципи ООП та патерни проєктування для створення системи управління бібліотекою.

---

## 📝 Завдання

### Частина 1: Інкапсуляція

Створіть структуру `Book` з наступними **приватними** полями:
- `title` (string)
- `author` (string)
- `isbn` (string)
- `available` (bool)

Реалізуйте:
- Конструктор `NewBook(title, author, isbn string) *Book`
- Getter методи для всіх полів
- Методи `Borrow()` та `Return()`

```go
book := NewBook("1984", "George Orwell", "978-0451524935")
book.Borrow()  // available = false
book.Return()  // available = true
```

### Частина 2: Поліморфізм

Створіть інтерфейс `LibraryItem`:
```go
type LibraryItem interface {
    GetTitle() string
    GetType() string
    CanBeBorrowed() bool
}
```

Реалізуйте його для:
- `Book` (можна позичити)
- `Magazine` (можна позичити тільки для читального залу)
- `ReferenceBook` (не можна позичити)

### Частина 3: Композиція

Створіть структуру `Library`:
```go
type Library struct {
    name  string
    items []LibraryItem
}
```

Методи:
- `AddItem(item LibraryItem)`
- `FindByTitle(title string) LibraryItem`
- `ListAvailableItems() []LibraryItem`

### Частина 4: Патерн Singleton

Реалізуйте `LibraryManager` як Singleton:
```go
manager := GetLibraryManager()
manager.RegisterLibrary("Central Library")
```

### Частина 5: Патерн Factory

Створіть Factory для створення різних типів LibraryItem:
```go
item := CreateLibraryItem("book", "1984", "George Orwell")
item := CreateLibraryItem("magazine", "National Geographic", "Various")
```

---

## ✅ Критерії успіху

- [ ] Всі поля структур приватні
- [ ] Реалізовано інтерфейс LibraryItem
- [ ] Використана композиція
- [ ] Singleton thread-safe
- [ ] Factory повертає правильні типи

---

## 💡 Підказки

1. Використовуйте `sync.Once` для Singleton
2. Type assertion для перевірки типу
3. Slice для зберігання items

---

## 🚀 Бонус

Додайте:
- `Member` структуру для користувачів
- Історію позичених книг
- Обмеження на кількість книг

---

**Час виконання:** 2-3 години
