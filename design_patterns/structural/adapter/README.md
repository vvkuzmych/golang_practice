# Adapter Pattern

## 📋 Опис

**Adapter** - структурний патерн, який дозволяє об'єктам з несумісними інтерфейсами працювати разом.

---

## 🎯 Проблема

- Два класи з несумісними інтерфейсами
- Не можете змінити існуючий код
- Потрібно використати сторонню бібліотеку
- Legacy код + новий код

**Приклад:**  
USB-C → USB-A адаптер

---

## ✅ Рішення

1. Створити адаптер клас
2. Адаптер реалізує потрібний інтерфейс
3. Всередині викликає методи адаптованого об'єкта
4. Перетворює дані якщо потрібно

---

## 🔧 Реалізація в Go

```go
// Цільовий інтерфейс
type MediaPlayer interface {
    Play(filename string)
}

// Існуючий клас (несумісний)
type VLCPlayer struct{}

func (v *VLCPlayer) PlayVLC(filename string) {
    fmt.Printf("Playing VLC: %s\n", filename)
}

// Адаптер
type VLCAdapter struct {
    vlc *VLCPlayer
}

func (a *VLCAdapter) Play(filename string) {
    a.vlc.PlayVLC(filename)
}

// Використання:
var player MediaPlayer = &VLCAdapter{vlc: &VLCPlayer{}}
player.Play("video.mp4")
```

---

## ✅ Переваги

- ✅ Працює з несумісним кодом
- ✅ Не змінює існуючі класи
- ✅ Single Responsibility Principle

## ❌ Недоліки

- ❌ Додатковий рівень абстракції
- ❌ Може бути складним

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Несумісні інтерфейси
- Сторонні бібліотеки
- Legacy code integration
- Різні формати даних

❌ **Не використовуйте коли:**
- Можете змінити вихідний код
- Прямий виклик простіше

---

## 🌍 Реальні приклади

### В Go stdlib:
- `io.Reader`, `io.Writer` адаптери
- `bufio.Reader` wraps `io.Reader`

### В реальних проектах:
- API wrappers
- Legacy system integration
- Different data format converters
- Third-party library adapters

---

## 💻 Запустити приклад

```bash
go run main.go
```

---

## 📚 Більше інформації

- [Refactoring.Guru - Adapter](https://refactoring.guru/design-patterns/adapter)

