# Builder Pattern

## 📋 Опис

**Builder** - породжуючий патерн, який дозволяє створювати складні об'єкти крок за кроком. Дозволяє використовувати той самий код для створення різних представлень об'єктів.

---

## 🎯 Проблема

- Конструктор з багатьма параметрами (5+)
- Багато optional параметрів
- Складний процес створення з багатьма кроками
- Потрібні різні представлення одного об'єкта

---

## ✅ Рішення

1. Винести створення в окремий клас Builder
2. Розбити створення на кроки (методи)
3. Викликати тільки потрібні кроки
4. Метод `Build()` повертає готовий об'єкт

---

## 🔧 Реалізація в Go

```go
type House struct {
    Walls   int
    Doors   int
    Windows int
    HasRoof bool
    HasGarage bool
}

type HouseBuilder struct {
    house House
}

func NewHouseBuilder() *HouseBuilder {
    return &HouseBuilder{}
}

func (b *HouseBuilder) SetWalls(n int) *HouseBuilder {
    b.house.Walls = n
    return b
}

func (b *HouseBuilder) SetDoors(n int) *HouseBuilder {
    b.house.Doors = n
    return b
}

func (b *HouseBuilder) Build() House {
    return b.house
}

// Використання:
house := NewHouseBuilder().
    SetWalls(4).
    SetDoors(2).
    SetWindows(6).
    Build()
```

---

## ✅ Переваги

- ✅ Зручно створювати складні об'єкти
- ✅ Fluent interface (method chaining)
- ✅ Immutable objects
- ✅ Не потрібні телескопічні конструктори

## ❌ Недоліки

- ❌ Більше коду
- ❌ Може бути надмірним для простих об'єктів

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Багато параметрів (5+)
- Багато optional параметрів
- Складний процес створення
- Потрібні immutable objects

❌ **Не використовуйте коли:**
- Простий об'єкт з 2-3 параметрами
- Всі параметри обов'язкові

---

## 🌍 Реальні приклади

### В Go stdlib:
- `strings.Builder` - будує strings
- `bytes.Buffer` - будує byte arrays
- `sql.DB` connection builders

### В реальних проектах:
- HTTP request builders
- Query builders (SQL)
- Configuration objects
- Complex UI components

---

## 💻 Запустити приклад

```bash
go run main.go
```

