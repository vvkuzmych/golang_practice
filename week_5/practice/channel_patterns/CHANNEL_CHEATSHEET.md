# 📚 Go Channels - Complete Cheat Sheet

**Всі типи каналів + як записувати та читати**

---

## 🎯 Quick Reference

| Тип каналу | Створення | Запис | Читання | Особливості |
|------------|-----------|-------|---------|-------------|
| **Unbuffered** | `make(chan T)` | `ch <- v` | `v := <-ch` | Блокує завжди |
| **Buffered** | `make(chan T, n)` | `ch <- v` | `v := <-ch` | Блокує лише коли буфер повний/порожній |
| **Send-only** | `chan<- T` | `ch <- v` | ❌ | Тільки запис |
| **Receive-only** | `<-chan T` | ❌ | `v := <-ch` | Тільки читання |
| **Closed** | `close(ch)` | ❌ panic | ✅ zero value | Не можна писати після close |
| **Nil** | `var ch chan T` | ⏸️ блокує навічно | ⏸️ блокує навічно | Корисно в select |

---

## 1️⃣ Unbuffered Channel (Небуферизований)

```go
// Створення
ch := make(chan int)

// Запис (блокується, поки хтось не прочитає)
ch <- 42

// Читання (блокується, поки хтось не запише)
value := <-ch
```

**Особливості:**
- ✅ Синхронізація: запис і читання одночасні
- ⏸️ Завжди блокує (потрібна пара)

---

## 2️⃣ Buffered Channel (Буферизований)

```go
// Створення з буфером на 3 елементи
ch := make(chan string, 3)

// Запис (не блокується, поки буфер не заповниться)
ch <- "first"
ch <- "second"
ch <- "third"
// ch <- "fourth" // ❌ заблокується!

// Читання (не блокується, поки буфер не спорожніє)
fmt.Println(<-ch) // "first"
fmt.Println(<-ch) // "second"
fmt.Println(<-ch) // "third"
// fmt.Println(<-ch) // ❌ заблокується!

// Перевірка стану
fmt.Println(len(ch)) // поточна кількість елементів
fmt.Println(cap(ch)) // ємність буфера
```

**Особливості:**
- ✅ Не блокує, поки є місце/елементи
- 📊 `len(ch)` - поточна кількість, `cap(ch)` - ємність

---

## 3️⃣ Send-Only Channel (Тільки запис)

```go
// Параметр функції: chan<- Type
func sendData(ch chan<- int) {
    ch <- 42  // ✅ можна писати
    // val := <-ch // ❌ ПОМИЛКА: не можна читати!
}

ch := make(chan int, 1)
sendData(ch)
```

**Синтаксис:** `chan<- Type` (стрілка після chan)

---

## 4️⃣ Receive-Only Channel (Тільки читання)

```go
// Параметр функції: <-chan Type
func receiveData(ch <-chan int) int {
    value := <-ch // ✅ можна читати
    // ch <- 42 // ❌ ПОМИЛКА: не можна писати!
    return value
}

ch := make(chan int, 1)
ch <- 100
result := receiveData(ch)
```

**Синтаксис:** `<-chan Type` (стрілка перед chan)

---

## 5️⃣ Closing Channel (Закриття каналу)

```go
ch := make(chan int, 2)
ch <- 1
ch <- 2

// Закриття
close(ch)

// ❌ ch <- 3 // panic: send on closed channel

// ✅ Читання з закритого каналу (повертає zero value)
fmt.Println(<-ch) // 1
fmt.Println(<-ch) // 2
fmt.Println(<-ch) // 0 (zero value)

// Перевірка, чи канал закритий
value, ok := <-ch
if !ok {
    fmt.Println("Channel closed")
}
```

**Правила:**
- ✅ Можна читати з закритого каналу (zero value)
- ❌ Не можна писати в закритий канал (panic)
- ❌ Не можна закрити nil канал (panic)

---

## 6️⃣ Range Over Channel (Ітерація)

```go
ch := make(chan int, 5)

go func() {
    for i := 1; i <= 5; i++ {
        ch <- i
    }
    close(ch) // ⚠️ ВАЖЛИВО: range зупиниться лише після close()!
}()

// Range автоматично читає, поки канал не закриється
for value := range ch {
    fmt.Println(value)
}
```

**Важливо:** `range` зупиниться ЛИШЕ після `close(ch)`!

---

## 7️⃣ Select (Вибір з кількох каналів)

```go
ch1 := make(chan string)
ch2 := make(chan string)

select {
case msg1 := <-ch1:
    fmt.Println("From ch1:", msg1)
case msg2 := <-ch2:
    fmt.Println("From ch2:", msg2)
case <-time.After(1 * time.Second):
    fmt.Println("Timeout!")
default:
    fmt.Println("No data ready")
}
```

**Особливості:**
- ⚡ Вибирає перший готовий case
- 🎲 Якщо кілька готові - вибирає випадково
- ⏰ `default` - виконується, якщо жоден канал не готовий

---

## 8️⃣ Nil Channel

```go
var ch chan int // nil канал (не ініціалізований)

fmt.Println(ch == nil) // true

// ⚠️ Операції з nil каналом:
// ch <- 1     // ⏸️ блокується навічно (deadlock!)
// val := <-ch // ⏸️ блокується навічно (deadlock!)
// close(ch)   // ❌ panic: close of nil channel
```

**Корисність:** в `select` для "вимкнення" каналу:

```go
select {
case val, ok := <-ch1:
    if !ok {
        ch1 = nil // Тепер select ігноруватиме ch1!
    }
case val := <-ch2:
    // ...
}
```

---

## 9️⃣ Channel of Channels (Канал каналів)

```go
// Канал, який передає інші канали
responseChan := make(chan chan int)

go func() {
    replyCh := make(chan int)
    responseChan <- replyCh // Передаємо канал
    result := <-replyCh      // Чекаємо відповідь
}()

// Отримуємо канал і відправляємо відповідь
ch := <-responseChan
ch <- 42
```

**Use case:** Request-response pattern

---

## 🔟 Bidirectional vs Unidirectional

```go
// Двонаправлений (можна читати ТА писати)
var biCh chan int = make(chan int)

// Однонаправлені
var sendOnly chan<- int    // тільки запис
var receiveOnly <-chan int // тільки читання

// Конвертація (автоматична)
sendOnly = biCh
receiveOnly = biCh
```

**Синтаксис:**
- `chan T` - bidirectional (read & write)
- `chan<- T` - send-only (write only)
- `<-chan T` - receive-only (read only)

---

## 📊 Comparison Table

| Операція | Unbuffered | Buffered | Closed | Nil |
|----------|------------|----------|--------|-----|
| **Запис** | блокує | блокує коли повний | ❌ panic | ⏸️ завжди блокує |
| **Читання** | блокує | блокує коли порожній | ✅ zero value | ⏸️ завжди блокує |
| **close()** | ✅ | ✅ | ❌ panic | ❌ panic |
| **len()** | 0 | 0-capacity | 0 | 0 |
| **cap()** | 0 | capacity | capacity | 0 |

---

## 🎓 Best Practices

### ✅ DO:
- Закривай канал на стороні **відправника** (sender)
- Використовуй `range` для читання всіх значень
- Використовуй `select` для таймаутів і множинних каналів
- Використовуй буферизовані канали для продуктивності

### ❌ DON'T:
- Не закривай канал на стороні **приймача** (receiver)
- Не пиши в закритий канал (panic!)
- Не закривай nil канал (panic!)
- Не закривай канал двічі (panic!)

---

## 🚨 Common Pitfalls

### 1. Deadlock з unbuffered channel:
```go
ch := make(chan int)
ch <- 42 // ❌ deadlock! (немає горутини для читання)
```

**Fix:**
```go
ch := make(chan int, 1) // buffered
ch <- 42 // ✅ OK
```

### 2. Забув close() з range:
```go
for val := range ch {
    // ⏸️ зависне, якщо ch не закритий!
}
```

**Fix:**
```go
close(ch) // в горутині, яка пише
```

### 3. Закрив канал двічі:
```go
close(ch)
close(ch) // ❌ panic: close of closed channel
```

---

## 📚 Examples File

Дивись повні приклади у файлі:  
`all_channel_types.go`

```bash
# Запустити всі приклади
go run all_channel_types.go
```

---

**Created:** 2026-01-19  
**Author:** AI Assistant  
**Project:** Golang Practice Week 5
