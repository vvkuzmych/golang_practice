# Command Pattern

## 📋 Опис

**Command** - поведінковий патерн, який перетворює запити в об'єкти, дозволяючи відкладати їх виконання, ставити в чергу, логувати і скасовувати.

---

## 🎯 Проблема

- Потрібно відкласти виконання операції
- Потрібен Undo/Redo
- Потрібно логувати операції
- Хочете передавати операції як параметри

**Приклад:**  
Кнопки Undo/Redo в текстовому редакторі

---

## ✅ Рішення

1. Інкапсулювати запит як об'єкт Command
2. Command має метод Execute()
3. Зберігати команди в історії
4. Можна відкладати, відміняти, повторювати

---

## 🔧 Реалізація в Go

```go
// Command інтерфейс
type Command interface {
    Execute()
    Undo()
}

// Конкретна команда
type LightOnCommand struct {
    light *Light
}

func (c *LightOnCommand) Execute() {
    c.light.On()
}

func (c *LightOnCommand) Undo() {
    c.light.Off()
}

// Invoker (кнопка)
type RemoteControl struct {
    command Command
}

func (r *RemoteControl) PressButton() {
    r.command.Execute()
}

// Використання:
light := &Light{}
command := &LightOnCommand{light: light}
remote := &RemoteControl{command: command}
remote.PressButton()
```

---

## ✅ Переваги

- ✅ Відокремлює відправника від отримувача
- ✅ Undo/Redo
- ✅ Черги команд
- ✅ Логування операцій

## ❌ Недоліки

- ❌ Багато класів команд
- ❌ Складність коду

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Потрібен Undo/Redo
- Черги задач
- Відкладене виконання
- Логування операцій

❌ **Не використовуйте коли:**
- Проста операція без історії
- Непотрібна додаткова складність

---

## 🌍 Реальні приклади

### В реальних проектах:
- Text editor (Undo/Redo)
- Task queues
- Database transactions
- UI button actions
- Macro recording

---

## 💻 Запустити приклад

```bash
go run main.go
```

