# Facade Pattern

## 📋 Опис

**Facade** - структурний патерн, який надає простий інтерфейс до складної системи класів, бібліотеки або фреймворку.

---

## 🎯 Проблема

- Складна система з багатьма класами
- Клієнту потрібна тільки частина функціональності
- Занадто багато кроків для простої операції

**Приклад:**  
Онлайн магазин: замовлення = (перевірка товару + оплата + доставка + нотифікація)

---

## ✅ Рішення

1. Створити Facade клас
2. Facade приховує складність
3. Надає простий інтерфейс
4. Делегує роботу підсистемам

---

## 🔧 Реалізація в Go

```go
// Складні підсистеми
type Inventory struct{}
func (i *Inventory) Check() bool { return true }

type Payment struct{}
func (p *Payment) Process() bool { return true }

type Shipping struct{}
func (s *Shipping) Ship() bool { return true }

// Facade
type OrderFacade struct {
    inventory *Inventory
    payment   *Payment
    shipping  *Shipping
}

func (o *OrderFacade) PlaceOrder() bool {
    return o.inventory.Check() &&
           o.payment.Process() &&
           o.shipping.Ship()
}

// Використання:
facade := &OrderFacade{...}
facade.PlaceOrder() // просто!
```

---

## ✅ Переваги

- ✅ Спрощує складну систему
- ✅ Зменшує залежності
- ✅ Легше використовувати

## ❌ Недоліки

- ❌ Може стати God Object
- ❌ Приховує деталі

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Складна система
- Потрібен простий інтерфейс
- Багато залежностей

❌ **Не використовуйте коли:**
- Система проста
- Потрібен повний контроль

---

## 🌍 Реальні приклади

### В Go stdlib:
- `http.ListenAndServe()` - facade для HTTP сервера
- `database/sql` - facade для DB драйверів

### В реальних проектах:
- E-commerce order processing
- Authentication systems
- Email service (SMTP setup)
- Cloud API wrappers

---

## 💻 Запустити приклад

```bash
go run main.go
```

