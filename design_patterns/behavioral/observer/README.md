# Observer Pattern

## 📋 Опис

**Observer** - поведінковий патерн, який створює механізм підписки, що дозволяє об'єктам отримувати сповіщення про зміни в інших об'єктах.

---

## 🎯 Проблема

- Об'єкти повинні знати про зміни в інших об'єктах
- Не хочете створювати тісну зв'язаність
- Кількість спостерігачів може змінюватися

**Приклад:**  
YouTube підписки - підписники отримують сповіщення про нові відео

---

## ✅ Рішення

1. Subject (видавець) тримає список Observer'ів
2. Observer'и підписуються на Subject
3. При зміні Subject сповіщає всіх Observer'ів
4. Observer'и можуть підписатись/відписатись

---

## 🔧 Реалізація в Go

```go
// Observer інтерфейс
type Observer interface {
    Update(message string)
}

// Subject (видавець)
type Subject struct {
    observers []Observer
}

func (s *Subject) Attach(observer Observer) {
    s.observers = append(s.observers, observer)
}

func (s *Subject) Notify(message string) {
    for _, observer := range s.observers {
        observer.Update(message)
    }
}

// Конкретний observer
type EmailSubscriber struct {
    email string
}

func (e *EmailSubscriber) Update(message string) {
    fmt.Printf("Email to %s: %s\n", e.email, message)
}

// Використання:
subject := &Subject{}
subject.Attach(&EmailSubscriber{email: "user@example.com"})
subject.Notify("New video!")
```

---

## ✅ Переваги

- ✅ Слабка зв'язаність
- ✅ Динамічні підписки
- ✅ Broadcast комунікація

## ❌ Недоліки

- ❌ Observer'и сповіщаються в випадковому порядку
- ❌ Можливі витоки пам'яті

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Зміни в одному об'єкті впливають на інші
- Невідома кількість підписників
- Pub/Sub pattern

❌ **Не використовуйте коли:**
- Проста залежність 1-1
- Синхронні оновлення критичні

---

## 🌍 Реальні приклади

### В Go stdlib:
- `context.Context` cancellation
- Channels (pub/sub)

### В реальних проектах:
- Event systems
- YouTube subscriptions
- Email newsletters
- Stock price updates
- Chat notifications

---

## 💻 Запустити приклад

```bash
go run main.go
```

---

## 📚 Більше інформації

- [Refactoring.Guru - Observer](https://refactoring.guru/design-patterns/observer)

