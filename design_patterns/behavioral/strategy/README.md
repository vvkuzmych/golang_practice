# Strategy Pattern

## 📋 Опис

**Strategy** - поведінковий патерн, який визначає сімейство алгоритмів, інкапсулює кожен з них і робить їх взаємозамінними.

---

## 🎯 Проблема

- Потрібно використовувати різні алгоритми
- Алгоритм вибирається в runtime
- Багато if/switch для вибору алгоритму
- Хочете додавати нові алгоритми без зміни існуючого коду

**Приклад:**  
Навігація: пішки, автомобілем, громадським транспортом

---

## ✅ Рішення

1. Створити інтерфейс для стратегії
2. Реалізувати різні стратегії
3. Контекст використовує стратегію через інтерфейс
4. Можна змінювати стратегію в runtime

---

## 🔧 Реалізація в Go

```go
// Інтерфейс стратегії
type PaymentStrategy interface {
    Pay(amount float64) string
}

// Конкретні стратегії
type CreditCardPayment struct{}
func (c *CreditCardPayment) Pay(amount float64) string {
    return fmt.Sprintf("Paid $%.2f with Credit Card", amount)
}

type PayPalPayment struct{}
func (p *PayPalPayment) Pay(amount float64) string {
    return fmt.Sprintf("Paid $%.2f with PayPal", amount)
}

// Контекст
type ShoppingCart struct {
    strategy PaymentStrategy
}

func (s *ShoppingCart) SetStrategy(strategy PaymentStrategy) {
    s.strategy = strategy
}

func (s *ShoppingCart) Checkout(amount float64) string {
    return s.strategy.Pay(amount)
}
```

---

## ✅ Переваги

- ✅ Взаємозамінні алгоритми
- ✅ Легко додавати нові стратегії
- ✅ Немає складних if/switch
- ✅ Open/Closed Principle

## ❌ Недоліки

- ❌ Більше класів
- ❌ Клієнт повинен знати про стратегії

---

## 🎓 Коли використовувати

✅ **Використовуйте коли:**
- Багато схожих класів з різними алгоритмами
- Потрібно змінювати алгоритм в runtime
- Складні умовні оператори для вибору алгоритму
- Приховати деталі реалізації алгоритму

❌ **Не використовуйте коли:**
- Алгоритм один і не змінюється
- Простий if достатньо

---

## 🌍 Реальні приклади

### В Go stdlib:
- `sort.Interface` - різні алгоритми сортування
- `io.Writer`, `io.Reader` - різні способи I/O
- `http.Handler` - різні обробники запитів

### В реальних проектах:
- Payment methods (Card, PayPal, Crypto)
- Shipping methods (Air, Sea, Ground)
- Compression algorithms (ZIP, GZIP, BZIP2)
- Authentication strategies (JWT, OAuth, Basic)

---

## 💻 Запустити приклад

```bash
go run main.go
```

---

## 📚 Більше інформації

- [Refactoring.Guru - Strategy](https://refactoring.guru/design-patterns/strategy)

